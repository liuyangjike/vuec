import {Center, install} from './center'
import {mapState, mapMutations, mapActions, mapGetters} from './helpers'
export default {
  Center, 
  install,
  mapState,
  mapMutations,
  mapActions,
  mapGetters
}